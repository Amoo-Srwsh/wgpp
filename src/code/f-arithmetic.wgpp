int $x$ = ARITH(2 add 2);
int $y$ = ARITH($x$ mul 2);
int $z$ = ARITH($y$ div $x$);
int $zyx$ = ARITH($x$ sub $y$ sub $z$);

printf "X: $x$\n";
printf "Y: $y$\n";
printf "Z: $z$\n";
printf "ZYX: $zyx$\n";
wout "Random (mod operation):";
wout ARITH($x$ mod 2);

exit ARITH($zyx$ mul -1);
